# Docling WebUI 产品需求文档 (PRD)

## 问题陈述

**谁在什么场景下遇到了什么痛点？**

企业内部的数据分析人员、财务人员、运营人员经常需要从大量扫描图片（发票、收据、报表、合同截图等）中提取表格数据。典型场景：每月收到上百张报销单照片、扫描的财务报表图片、供应商发来的报价单截图，需要将图片中的表格数据整理成结构化格式（Excel/CSV）进行汇总分析。

**现在怎么解决的？**

1. 手动录入：逐张图片人工看、人工敲数据，10张图片可能要花2小时，且容易出错。
2. 传统OCR工具（如Tesseract、PaddleOCR）：能识别文字，但表格结构经常被打乱——合并单元格丢失、行列错位、多列表格被读成一行文本。
3. 在线OCR服务（如OnlineOCR.net）：一次只能处理一张，不支持批量ZIP上传，数据要上传到第三方服务器存在隐私风险。
4. Docling库本身：OCR和表格提取能力强，但只有API和开发者接口，没有面向非技术用户的WebUI；自带的Gradio UI功能简陋，不支持批量ZIP上传、不支持LLM后处理整理表格、不支持原始图片与解析结果合并导出。

**为什么不行？**

- 手动录入效率极低，无法应对批量场景
- 传统OCR不理解表格结构，输出需要大量人工修正
- 在线工具有隐私风险且不支持批量
- Docling虽有技术能力但缺乏易用界面和LLM后处理能力
- 没有任何现有工具能同时做到：批量ZIP上传 + OCR表格提取 + LLM整理结构化数据 + 原始图片与结果合并导出

---

## 目标用户

### 主要用户画像
- **角色**：企业数据分析人员 / 财务专员 / 运营专员
- **年龄**：25-45岁
- **技术水平**：熟练使用Excel和Web应用，不写代码，不会配置Python环境
- **场景**：每月定期收到几十到上百张含表格的扫描图片（报销单、发票、报表），需要批量提取表格数据并整理成结构化格式
- **痛点**：手动录入耗时、传统OCR输出混乱、需要对照原图核对数据准确性
- **期望**：上传一个ZIP包，等几分钟，下载包含结构化表格和原图的打包结果

### 次要用户画像
- **角色**：开发人员 / IT运维人员
- **年龄**：25-40岁
- **技术水平**：熟悉API调用，了解Docker部署
- **场景**：已部署Docling Serve，需要一个可视化界面进行临时批量处理，而非每次写脚本
- **痛点**：Docling Serve API功能完备但缺乏前端界面，临时处理需写代码
- **期望**：部署即用的Web界面，能直接对接已部署的Docling Serve和OpenAI兼容LLM API

---

## 竞品分析

| 竞品 | 核心功能 | 优势 | 劣势（来自差评/用户反馈） | 定价 |
|------|----------|------|--------------------------|------|
| **Docling（IBM）** | 文档转Markdown/JSON/HTML，支持30+格式，表格提取，OCR | 开源Apache 2.0免费；布局分析和阅读顺序识别强；MCP协议支持；本地部署数据不出环境 | 无面向非技术用户的UI；API频繁变更需版本锁定；批量处理时内存增长问题；无LLM后处理能力；无ZIP批量导入；无原图+结果合并导出 | 免费/开源 |
| **MinerU（OpenDataLab）** | 工业级文档解析，37种语言，表格/公式/图片提取，输出Markdown/JSON/HTML | OmniDocBench综合分95.69行业第一；支持批量(最多200文件)；MCP协议；Notion/Dify导出 | API有页数限制(600页)和日配额(2000页高优先级)；需Token；ZIP输出但不含原图；无LLM后处理整理表格；本地部署复杂需GPU | 免费有配额限制，付费扩容 |
| **PaddleOCR + PP-StructureV3** | OCR引擎+表格结构提取，输出Excel/JSON/HTML | 74k Stars社区成熟；中文识别率高；表格还原支持 | 合并单元格复杂表格(银行对账单)识别不准；无内置WebUI批量处理；需编程知识；无ZIP导入；无原图合并导出；2.x/3.x版本API不兼容 | 免费/开源 |
| **DeepSeek-OCR** | 端到端OCR-free Transformer，支持文字/图表/公式识别 | 100+语言；HTML/Markdown表格输出；内存高效 | 需GPU(8-10GB起步)；偶发幻觉(编造不存在的内容)；无内置批量WebUI；无LLM后处理；无ZIP导入导出；手写体识别弱 | 开源MIT免费 |
| **Nanonets-OCR2 / DocExt** | VLM文档提取，结构化Markdown，字段+表格双轨抽取 | 零OCR依赖避免错误传播；本地部署；置信度评分；Gradio Web界面；多模型接入 | 需GPU；无ZIP批量处理；无LLM后处理整理表格；无原图+结果合并导出；Gradio UI功能简陋；偶发幻觉风险 | 开源免费 |

### 替代方案分析

| 替代方案 | 核心功能 | 优势 | 劣势 | 定价 |
|----------|----------|------|------|------|
| **商业云OCR服务（AWS Textract / Azure Document Intelligence / Google Document AI）** | 云端文档智能，表格提取，结构化JSON输出 | 精度97-99%；无需运维；弹性扩容 | $1,500-$50,000/百万页；数据离开环境有隐私风险；有速率限制；不支持ZIP批量原图+结果合并导出；无LLM后处理 | 按页付费，昂贵 |
| **桌面OCR工具（ABBYY FineReader / Adobe Acrobat Pro / WPS Office）** | 桌面OCR，表格识别，Excel导出 | 用户友好；精度高；Excel直接导出 | 单机授权$100-$300；不支持批量ZIP；无LLM集成；无API自动化；无原图合并导出；不支持服务器部署 | 一次性授权费 |

---

## 我们的差异化

用户为什么选我们而不是竞品？

1. **OCR + LLM双引擎**：Docling Serve负责高精度OCR和表格结构提取，LLM负责后处理——修正OCR错误、统一表格格式、补全断裂的单元格内容。这是市场上唯一将OCR引擎和LLM后处理整合在一个WebUI中的方案。

2. **ZIP批量处理**：用户上传一个ZIP压缩包，系统自动解压、逐张识别、批量处理。竞品要么不支持ZIP（Docling/PaddleOCR/DeepSeek-OCR），要么有文件数量限制（MinerU最多200个），且都需要额外脚本才能实现批量。

3. **原图 + 解析结果合并导出**：导出的ZIP包含每张原始图片和对应的解析结果（表格Excel/CSV + Markdown），方便人工核对。没有任何竞品提供此功能。

4. **部署即用，无需登录**：个人/内部工具，Docker部署后直接访问Web界面，输入LLM API Key即可使用。不需要注册、不需要Token、不需要配额管理。

5. **对接已有Docling Serve**：不重复造轮子，直接对接用户已部署的Docling Serve API，在其基础上增加WebUI和LLM后处理层。

---

## 核心功能（RICE 排序）

| 功能 | Reach | Impact | Confidence | Effort | Score | MVP? |
|------|-------|--------|------------|--------|-------|------|
| **图片上传与ZIP批量解压** | 10 | 3 | 100% | 3 | 10.0 | 是 |
| **Docling Serve API对接（单张/批量转换）** | 10 | 3 | 100% | 3 | 10.0 | 是 |
| **LLM表格后处理（OpenAI兼容API）** | 10 | 3 | 80% | 4 | 6.0 | 是 |
| **任务列表与状态追踪** | 10 | 2 | 100% | 2 | 10.0 | 是 |
| **结果导出（ZIP含原图+解析结果）** | 10 | 3 | 100% | 3 | 10.0 | 是 |
| **单张图片预览与结果对比查看** | 8 | 2 | 100% | 2 | 8.0 | 是 |
| **OCR/表格模式配置（fast/accurate）** | 6 | 1 | 100% | 1 | 6.0 | 否 |
| **LLM自定义提示词配置** | 5 | 1 | 80% | 2 | 2.0 | 否 |
| **解析历史记录与搜索** | 4 | 0.5 | 80% | 3 | 0.53 | 否 |
| **多用户任务隔离（基于Session）** | 3 | 0.5 | 80% | 3 | 0.4 | 否 |

> RICE Score = (Reach x Impact x Confidence) / Effort

---

## MVP 范围

MVP仅包含RICE评分最高的6个功能（Score >= 6.0）：

1. **图片上传与ZIP批量解压**：支持单张/多张图片上传，支持ZIP压缩包上传后自动解压
2. **Docling Serve API对接**：调用已部署的Docling Serve进行OCR和表格结构提取，支持异步轮询和WebSocket状态订阅
3. **LLM表格后处理**：将Docling的OCR结果送入OpenAI兼容LLM，按预设提示词整理表格数据（修正错位、补全缺失、统一格式）
4. **任务列表与状态追踪**：展示所有提交的解析任务，实时显示处理状态（排队中/OCR中/LLM处理中/已完成/失败）
5. **结果导出（ZIP含原图+解析结果）**：一键导出ZIP包，包含原始图片、解析的Markdown/JSON、LLM整理后的表格（Excel/CSV）
6. **单张图片预览与结果对比查看**：在Web界面中左右分栏查看原始图片和解析结果

### Backlog（后续迭代）
- OCR/表格模式配置（fast/accurate切换）
- LLM自定义提示词配置界面
- 解析历史记录与搜索
- 多用户任务隔离
- 批量PDF处理（当前MVP聚焦图片）
- 结果在线编辑与修正

---

## 验收标准（Given/When/Then）

### F1: 图片上传与ZIP批量解压
- **Given** 用户在Web界面的上传区域，**When** 用户拖拽或选择1张或多张图片（PNG/JPEG/TIFF/BMP/WebP），**Then** 系统显示文件列表，每张图片显示文件名、大小和缩略图预览
- **Given** 用户在上传区域，**When** 用户上传一个ZIP压缩包，**Then** 系统自动解压并列出包内所有支持的图片格式文件，忽略非图片文件
- **Given** 用户已选择文件，**When** 用户点击"开始解析"按钮，**Then** 系统为每张图片创建一个解析任务并跳转到任务列表页
- **Given** ZIP包内包含50张图片，**When** 系统解压后，**Then** 列表正确显示50张图片，文件名保持原始名称
- **Given** 用户上传的ZIP包为空或不含支持的图片格式，**When** 系统尝试解压，**Then** 显示明确的错误提示"未在压缩包中找到支持的图片格式"

### F2: Docling Serve API对接
- **Given** Docling Serve运行在 http://localhost:5001，**When** 用户提交图片进行解析，**Then** 系统调用 POST /v1/convert/file（异步模式），传入图片文件和转换参数（do_ocr=true, do_table_structure=true, to_formats=["md","json"]）
- **Given** 异步任务已提交并返回task_id，**When** 系统轮询 GET /v1/status/poll/{task_id}（或WebSocket订阅），**Then** 任务列表中对应任务状态实时更新为"OCR处理中"
- **Given** Docling任务返回结果，**When** 系统获取 GET /v1/result/{task_id}，**Then** 解析结果（Markdown/JSON）被保存到任务记录中
- **Given** Docling Serve不可达（服务未启动或地址错误），**When** 用户提交解析任务，**Then** 系统显示错误提示"无法连接到Docling Serve服务，请检查服务地址"并标记任务为失败
- **Given** 单张图片解析超时（超过5分钟未返回），**When** 系统检测到超时，**Then** 自动标记任务为"超时"并允许用户重试

### F3: LLM表格后处理
- **Given** Docling返回的Markdown/JSON结果中包含表格，**When** 系统将结果发送到OpenAI兼容LLM API，**Then** LLM按预设提示词整理表格数据，返回结构化的表格格式（修正OCR错位、补全缺失单元格、统一数据格式）
- **Given** 环境变量中配置了有效的LLM API Key和Base URL，**When** LLM处理完成，**Then** 整理后的表格数据以Excel格式（.xlsx）和CSV格式保存
- **Given** LLM API不可达或返回错误，**When** 系统捕获到LLM处理失败，**Then** 保留Docling原始OCR结果，标记任务为"LLM处理失败，保留原始OCR结果"，并允许用户跳过LLM步骤直接导出
- **Given** 图片中不包含表格（纯文本或图片），**When** 系统检测到OCR结果中无表格结构，**Then** 跳过LLM表格处理步骤，直接保存文本结果

### F4: 任务列表与状态追踪
- **Given** 用户已提交多个解析任务，**When** 用户访问任务列表页，**Then** 看到所有任务的列表，每条显示：文件名、缩略图、提交时间、当前状态（排队中/OCR处理中/LLM处理中/已完成/失败/超时）、操作按钮
- **Given** 任务正在处理中，**When** 系统接收到状态更新（通过轮询或WebSocket），**Then** 任务列表实时更新状态，无需手动刷新
- **Given** 任务已完成，**When** 用户查看任务详情，**Then** 可看到解析结果（Markdown预览 + 表格预览）和原始图片对比
- **Given** 某个任务失败，**When** 用户点击失败任务，**Then** 可看到错误原因并提供"重试"按钮

### F5: 结果导出
- **Given** 一个或多个任务已完成，**When** 用户选择任务并点击"导出"按钮，**Then** 系统生成一个ZIP压缩包，包含：原始图片文件、解析结果（Markdown + JSON）、LLM整理后的表格（Excel + CSV）
- **Given** 导出的ZIP包结构，**When** 用户解压ZIP，**Then** 每张图片对应一个文件夹，文件夹内包含：原始图片（保留原文件名）、result.md（Markdown解析结果）、result.json（JSON解析结果）、table.xlsx（LLM整理的表格）、table.csv（CSV格式表格）
- **Given** 用户选择多个任务批量导出，**When** 系统生成ZIP，**Then** ZIP内每个任务一个子文件夹，结构清晰可对照

### F6: 单张图片预览与结果对比
- **Given** 任务已完成，**When** 用户在任务列表中点击某个任务，**Then** 进入详情页，左侧显示原始图片，右侧显示解析结果（Markdown文本 + 表格渲染）
- **Given** 解析结果中包含表格，**When** 表格在右侧渲染显示，**Then** 表格保持行列结构，表头加粗，可滚动查看
- **Given** 原始图片较大，**When** 用户在详情页查看，**Then** 图片支持缩放和拖动查看

---

## 边界条件

| 场景 | 处理方式 |
|------|----------|
| **空状态** | 任务列表为空时显示引导提示"上传图片或ZIP压缩包开始解析"，附带上传按钮 |
| **ZIP包为空** | 显示错误"压缩包内未找到支持的图片文件"，不创建任务 |
| **不支持的视频格式** | 过滤非图片文件，仅处理PNG/JPEG/TIFF/BMP/WebP，忽略其他文件 |
| **超大文件** | 单张图片超过20MB时提示"图片过大可能影响处理速度"，仍允许上传但建议压缩 |
| **超大ZIP包** | ZIP包内图片超过100张时提示"图片数量较多，处理时间可能较长" |
| **Docling Serve离线** | 显示连接状态指示器（红/绿点），离线时禁用上传按钮并提示"Docling Serve未连接" |
| **LLM API未配置** | 显示配置提示"请设置LLM API Key"，允许仅使用OCR模式（跳过LLM后处理） |
| **网络中断** | 任务处理中网络中断时标记为"网络异常"，支持自动重试（最多3次） |
| **并发上传** | 同时提交超过20个任务时排队处理，显示队列位置 |
| **权限拒绝** | 文件读取失败时标记为"文件读取错误"，不影响其他任务 |
| **加载状态** | 任务处理中显示加载动画和当前步骤文字（如"正在OCR识别..." "正在LLM整理表格..."）|
| **OCR结果无表格** | 跳过LLM步骤，直接保存文本结果，任务标记为"已完成（无表格）" |

---

## 非功能需求

| 类别 | 要求 | 优先级 |
|------|------|--------|
| 性能 | 单张图片OCR+LLM处理 < 30秒（不含排队时间）；页面首屏加载 < 3秒；API请求p95 < 500ms | P0 |
| 可用性 | Docling Serve不可用时降级为只读模式（查看历史结果）；LLM不可用时降级为仅OCR模式 | P1 |
| 安全 | 部署在内网环境；LLM API Key通过环境变量配置不硬编码；HTTPS部署（如需外网访问）；输入文件校验防路径穿越 | P0 |
| 兼容性 | Chrome/Safari/Firefox最新2个版本；移动端适配（响应式布局，最低支持iPhone SE以上尺寸） | P0 |
| 可访问性 | WCAG 2.1 AA基本合规（键盘可达核心操作、颜色对比度4.5:1以上） | P2 |
| 国际化 | 界面默认中文，预留i18n接口（后续可扩展英文） | P2 |
| 数据埋点 | 核心流程埋点（上传/解析开始/解析完成/导出/错误） | P1 |
| 部署 | Docker Compose一键部署；支持环境变量配置Docling Serve地址和LLM API参数 | P0 |

---

## 数据埋点方案

### MVP必埋点关键事件

| 事件类别 | 必埋事件 | 说明 |
|----------|----------|------|
| 获客 | page_view, upload_started | 用户访问页面、开始上传文件 |
| 激活 | first_parse_complete | 用户完成第一次图片解析（核心价值验证点） |
| 留存 | session_start, session_duration | 每次会话开始、会话时长 |
| 转化 | export_clicked, export_completed | 用户点击导出、导出完成 |
| 异常 | ocr_error, llm_error, parse_timeout | OCR错误、LLM错误、解析超时 |

### 埋点实现要求
- 前端用轻量自建 `trackEvent()` 封装（不依赖第三方SDK，内部工具无需外部分析平台）
- 事件命名规范：`{对象}_{动作}`（如 `upload_started`, `parse_completed`, `export_clicked`）
- 属性规范：每个事件附带 `timestamp`, `file_count`, `file_type`, `parse_mode`
- 不采集隐私数据（不上报IP、不存原始文件内容、不存LLM API Key）

---

## 界面方案说明（图标规范）

- 所有功能图标使用统一SVG图标库（具体由架构师按项目选型锁定，不预设具体库）
- 图标描述采用文字说明方式，如"上传图标"、"下载图标"、"刷新图标"、"错误警示图标"
- 不使用emoji作为功能图标
- 界面配色避免紫色到粉色的渐变方案，采用中性色调为主（白色背景、深灰文字、蓝色主色调用于按钮和链接）

---

## 技术依赖（供架构师参考）

| 依赖项 | 说明 |
|--------|------|
| Docling Serve API | 已部署在 http://localhost:5001，提供 /v1/convert/file/async, /v1/status/poll/{task_id}, /v1/result/{task_id} |
| OpenAI兼容LLM API | 用户配置API Key和Base URL，用于表格数据后处理 |
| ZIP处理 | 前端上传ZIP，后端解压；导出时后端打包ZIP |
| 图片格式 | 支持PNG, JPEG, TIFF, BMP, WebP |
